export type Project = {
  title: string;
  tags: string[];
  imageLink?: string;
  description: string;
  codeLink?: string;
  viewLink?: string;
};

export type DoubleLenguage = {
  portuguese: string;
  english: string;
  main: string;
};
